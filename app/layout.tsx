import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import Navbar from "@/components/navbar"
import Footer from "@/components/footer"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Graha Asri Residence - Modern Living in Peaceful Neighborhood",
  description:
    "Discover your dream home at Graha Asri Residence. Modern housing complex with strategic location, 24/7 security, and green environment. Ready to live in.",
  keywords: "housing, residence, property, modern living, peaceful neighborhood",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navbar />
        <main className="min-h-screen">{children}</main>
        <Footer />
      </body>
    </html>
  )
}
